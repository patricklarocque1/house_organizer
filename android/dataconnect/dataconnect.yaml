specVersion: "v1"
serviceId: "app"
location: "us-central1"
schema:
  # Android module-local view of the real schema folder.
  source: "../../dataconnect/schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "app-fdc"
        # schemaValidation: "STRICT"
        # schemaValidation: "COMPATIBLE"
connectorDirs: ["../../dataconnect/example"]
